# ════════════════════════════════════════════════════════════════
# glossary .env 설정 파일
# 이 파일을 복사해서 .env 로 저장 후 값을 채워주세요
# .env 는 .gitignore 에 의해 git에 포함되지 않습니다
# ════════════════════════════════════════════════════════════════

# ── 프로젝트 도메인 설정 (AI 문맥용) ─────────────────────────
# 기본값으로 설정되어 있습니다. 타 도메인이나 프로젝트에 사용 시 변경하세요.
# GLOSSARY_PROJ_NAME="BOM_TS"
# GLOSSARY_PROJ_DOMAIN="an automated stock/FX trading system"


# ── API 설정 ─────────────────────────────────────────────────────
# 사용할 API 종류: claude | openai | google
API_KEY_TYPE=claude

# 사용할 모델 (비워두면 API별 기본값 사용)
# claude : claude-sonnet-4-20250514 / claude-opus-4-6 / claude-haiku-4-5-20251001
# openai : gpt-4o / gpt-4o-mini / gpt-4-turbo
# google : gemini-2.0-flash / gemini-1.5-pro / gemini-1.5-flash
API_MODEL=

ANTHROPIC_API_KEY=sk-ant-여기에입력
GOOGLE_API_KEY=여기에입력
OPENAI_API_KEY=여기에입력

# ── 프로젝트 경로 ────────────────────────────────────────────────
# 비워두면 glossary/ 의 상위 폴더를 자동 감지
# Windows 예시: E:\project\antigravity
# Mac/Linux 예시: /home/user/projects/antigravity
PROJ_ROOT=

# ── 스캔 제외 설정 ───────────────────────────────────────────────
# 완전 제외 (내용도 안 봄, 폴더명도 후보로 안 넣음)
EXCLUDE_DIRS=backup,data,tests,lib_test,tmp,glossary,.git,__pycache__,node_modules,.venv,venv

# 내용은 안 보고 파일명만 후보로 사용하는 폴더
EXCLUDE_FILE_CONTENT=cache,doc,log

# 내용 스캔 제외 확장자
EXCLUDE_EXTENSIONS=.md,.txt,.log,.csv,.png,.jpg,.jpeg,.pdf,.ico,.svg

# ── 알림 설정 (§5.3 Alerting) ──────────────────────────────────────
# Telegram 봇 알림 (generate FATAL / validate CRITICAL / commit 완료 시 전송)
TELEGRAM_SYSTEM_TOKEN=여기에입력   # 형식: 숫자:영문자 (BotFather에서 발급)
TELEGRAM_DEFAULT_CHAT_ID=여기에입력  # 개인 chat_id 또는 그룹 chat_id

# Slack 알림 — 방식 1 (권장): Incoming Webhook URL
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/여기에입력
#
# Slack 알림 — 방식 2: Bot Token (xoxb- 로 시작해야 함)
SLACK_BOT_TOKEN=xoxb-여기에입력
SLACK_DEFAULT_CHAT_ID=C여기에입력  # Slack 채널 ID (예: C04XXXXXYYY)
#
# ※ SLACK_WEBHOOK_URL이 설정되어 있으면 Bot Token 방식은 무시됩니다.
# ※ 알림 설정을 비워두면 알림 기능은 비활성화됩니다.

# ── Trading Freeze 설정 (§1.3) ─────────────────────────────────────
# 장중 배포 방지: 1=활성화(기본), 0=비활성화 (개발/테스트 시 0으로 변경)
TRADING_FREEZE_ENABLED=1
# 코인(Upbit) 포지션 보유 시 상시 배포 차단: 1=차단, 0=허용(기본)
TRADING_FREEZE_CRYPTO=0

# ── 배치 설정 ────────────────────────────────────────────────────
# 한 번에 API에 넘길 용어 후보 수 (너무 크면 토큰 폭발 주의)
BATCH_CHUNK_SIZE=300

# API 응답 최대 토큰 (4000이면 JSON이 잘릴 수 있음 → 8192 권장)
MAX_OUTPUT_TOKENS=8192


## # 1. .env 생성
## cp .env.example .env
## # .env 열어서 API_KEY_TYPE, API키, PROJ_ROOT 입력
## 
## # 2. 서버 실행
## python glossary/web/server.py

## 1. 스캔 탭
##    → "▷ 스캔 실행"   : API 없이 후보 추출, 미리보기
##    → "▷ dry-run"     : 토큰 예상치 확인 (API 미호출)
##    → "▷ API 분석 실행" : 실제 분석 → tmp/terms/terms_날짜.json 생성
## 
## 2. 결과 파일 탭
##    → 생성된 파일 목록 확인
##    → "병합 →" 버튼으로 3번 탭 이동
## 
## 3. 병합 탭
##    → 추출된 용어 체크박스로 선택/해제
##    → "전체 병합" 또는 "선택 항목만 병합"
##    → terms.json 에 즉시 반영

## # 후보 수만 확인
## python glossary/bin/scan_terms.py --count
## 
## # 후보 목록 상세 보기
## python glossary/bin/scan_terms.py
## 
## # 토큰 예상치 확인 (API 미호출)
## python glossary/bin/batch_terms.py --dry-run
## 
## # 실제 실행
## python glossary/bin/batch_terms.py