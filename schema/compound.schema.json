{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Compound",
  "description": "BOM_TS 복합어 사전 항목 — Plan v2.5.1 §3.3. abbr 필드 제거, variants array 통합.",
  "type": "object",
  "required": [
    "id",
    "words",
    "lang",
    "domain",
    "status",
    "created_at",
    "updated_at"
  ],
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^(\\[N\\])?[a-z][a-z0-9_]*(\\[N\\])?[a-z0-9_]*$|^[a-z][a-z0-9_]*(\\[N\\])[a-z0-9_]*$"
    },
    "words": {
      "type": "array",
      "items": { "type": "string" },
      "minItems": 1
    },
    "domain": {
      "anyOf": [
        { "type": "string", "enum": ["trading", "market", "system", "infra", "ui", "general", "proper"] },
        { "type": "array", "items": { "type": "string", "enum": ["trading", "market", "system", "infra", "ui", "general", "proper"] } }
      ]
    },
    "status": {
      "type": "string",
      "enum": ["active", "deprecated"],
      "default": "active"
    },
    "lang": {
      "type": "object",
      "properties": {
        "en":      { "type": "string" },
        "ko":      { "type": "string" },
        "ja":      { "type": "string" },
        "zh_hans": { "type": "string" }
      },
      "required": ["en"]
    },
    "variants": {
      "description": "§3.2 지원 타입 전체. abbreviation에 long/short 포함 (구 abbr 객체 통합). §3.4 정렬: abbreviation > alias > plural > 기타",
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "type": {
            "type": "string",
            "enum": [
              "plural", "singular", "abbreviation", "alias",
              "misspelling", "adjective", "adverb", "verb",
              "noun_form", "verb_form", "present_participle",
              "past", "past_participle", "agent", "pos_forms", "deprecated"
            ]
          },
          "types": {
            "type": "array",
            "items": {
              "type": "string",
              "enum": [
                "plural", "singular", "abbreviation", "alias",
                "misspelling", "adjective", "adverb", "verb",
                "noun_form", "verb_form", "present_participle",
                "past", "past_participle", "agent", "pos_forms", "deprecated"
              ]
            }
          },
          "value":  { "type": "string" },
          "short":  { "type": "string" },
          "long":   { "type": "string" },
          "domain": { "type": "string" },
          "noun":   { "type": "array", "items": { "type": "string" } },
          "verb":   { "type": "array", "items": { "type": "string" } },
          "adj":    { "type": "array", "items": { "type": "string" } },
          "adv":    { "type": "array", "items": { "type": "string" } },
          "abbreviation_meta": {
            "type": "object",
            "properties": {
              "confidence": { "enum": ["high", "medium", "low"] },
              "source":     { "enum": ["dictionary", "api", "ai", "manual"] },
              "ambiguity":  { "enum": ["low", "medium", "high"] }
            }
          }
        },
        "anyOf": [
          { "required": ["type"] },
          { "required": ["types"] }
        ],
        "oneOf": [
          { "required": ["value"] },
          { "required": ["short"] }
        ]
      }
    },
    "description_i18n": {
      "type": "object",
      "properties": {
        "ko": { "type": "string" },
        "en": { "type": "string" }
      }
    },
    "reason": {
      "type": "string",
      "minLength": 1
    },
    "not": {
      "type": "array",
      "items": { "type": "string" }
    },
    "created_at":    { "type": "string", "format": "date-time" },
    "updated_at":    { "type": "string", "format": "date-time" },
    "abbreviation": {
      "type": "object",
      "properties": {
        "short":          { "type": "string",  "description": "약어 표기 (예: BOMTS)" },
        "long":           { "type": "string",  "description": "풀네임 compound id (예: bomiyang_trading_system)" },
        "case_sensitive": { "type": "boolean", "default": false },
        "confidence":     { "type": "string",  "enum": ["high", "medium", "low"] },
        "ambiguity":      { "type": "string",  "enum": ["low", "medium", "high"] }
      },
      "required": ["short"],
      "additionalProperties": false
    },
    "deprecated_at": { "type": "string", "format": "date-time" }
  },
  "additionalProperties": false
}